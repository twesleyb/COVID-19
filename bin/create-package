#!/usr/bin/env Rscript

# Create an R package in the current directory.
# The current directory is used as the name. 
# Usually, I execute this from project_root/bin, 
# so the script assumes that you are here.

# Get path of project's root directory.
here <- getwd()
bin <- here
root <- dirname(bin)

# Create a package of that name.
devtools::create(root)
